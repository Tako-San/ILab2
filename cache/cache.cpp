#include "cache.h"


int main()
{
    /*int n;
    size_t m;

    std::cin >> m >> n; // scanf("%d%d", &n, &m);
    cache_t <page_t> c{m};

    int hits = 0;
    for (int i = 0; i < n; ++i)
    {
        page_t p{};
        std::cin >> p.id; // scanf("%d", &q);
        assert(std::cin.good());
        if (c.lookup(&p))
            hits += 1;
    }
    std::cout << hits << "\n"; // printf("%d\n", n);*/
}
